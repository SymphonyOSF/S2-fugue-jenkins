# S2-fugue-jenkins
Docker image build for local CICD server

Clone this repo

```
$ mkdir /tmp/git
$ cd /tmp/git
$ git clone S2-fugue-jenkins
$ cd S2-fugue-jenkins
$
```

Build the docker image

```
$ ./build.sh
```

Run the image

```
$ ./run.sh
```
